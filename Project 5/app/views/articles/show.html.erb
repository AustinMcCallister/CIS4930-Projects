<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  <li><%= link_to "Destroy", article_path(@article), data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
  } %></li> <%# turbo method: :delete causes the link to make a DELETE request instead of a GET %>
</ul>

<h2>Comments</h2>
<%= render @article.comments %> <%# renders from partials located in comments directory %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>
